void setup(){

size(1000, 1000);

smooth();
background(0);

mouseX=width/2; mouseY=height/2;

}

void draw(){
  float r =random(200);
  
  fill(234,0,random(120));
  noStroke();
  
  ellipse(mouseX, mouseY, r, r);
  
}
